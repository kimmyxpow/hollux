{
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.1",
    "tailwindcss": "^3.0.22"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.4.0"
  },
  "name": "fe-hotelhebat",
  "version": "1.0.0",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i tailwind.css -o style.css --watch",
    "minify": "npx tailwindcss -i tailwind.css -o style.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
